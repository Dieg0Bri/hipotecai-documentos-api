name: deploy-prod

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-prod-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: prod
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}
      - uses: google-github-actions/setup-gcloud@v2

      - name: Resolve OCR_API_URL (prod)
        id: ocr
        run: |
          OCR=$(gcloud run services describe ocr-api --region=southamerica-west1 --project=${{ secrets.GCP_PROJECT }} --format='value(status.url)' 2>/dev/null || echo "")
          echo "url=$OCR" >> $GITHUB_OUTPUT

      - run: |
          gcloud builds submit . \
            --config=cloudbuild-prod.yaml \
            --project=${{ secrets.GCP_PROJECT }} \
            --substitutions=_GOOGLE_CLIENT_ID="${{ secrets.GOOGLE_CLIENT_ID }}",_OCR_API_URL="${{ steps.ocr.outputs.url }}" \
            --polling-interval=15

      - id: url
        run: |
          URL=$(gcloud run services describe documentos-api --region=southamerica-west1 --project=${{ secrets.GCP_PROJECT }} --format='value(status.url)')
          echo "service_url=$URL" >> $GITHUB_OUTPUT
      - name: Smoke test
        run: |
          set -e
          STATUS=$(curl -s -o /tmp/h.json -w "%{http_code}" "${{ steps.url.outputs.service_url }}/health")
          echo "HTTP $STATUS"; cat /tmp/h.json
          [ "$STATUS" = "200" ]
